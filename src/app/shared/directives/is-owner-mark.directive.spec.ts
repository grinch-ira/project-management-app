import { IsOwnerMarkDirective } from './is-owner-mark.directive';

describe('IsOwnerMarkDirective', () => {
  it('should create an instance', () => {
    const directive = new IsOwnerMarkDirective();
    expect(directive).toBeTruthy();
  });
});
